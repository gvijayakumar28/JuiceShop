# Browser to use: chrome | firefox | edge
browser=chrome

# Execution mode: local | remote
execution=local

# Headless mode (no visible browser window): true | false
headless=false

# Incognito / private mode: true | false
incognito=false

# Selenium Grid URL (only used when execution=remote)
grid.url=http://localhost:4444/wd/hub
