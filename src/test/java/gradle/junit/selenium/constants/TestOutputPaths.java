package gradle.junit.selenium.constants;

public class TestOutputPaths {

    // Root folder for all test output
    public static final String ROOT = "test-output";

    // Folder where screenshot PNG files are saved
    public static final String SCREENSHOTS = ROOT + "/screenshots";

    // Folder where log files are saved
    public static final String LOGS = ROOT + "/logs";

    // Folder where Allure HTML report is generated
    public static final String REPORTS = ROOT + "/reports";
}
